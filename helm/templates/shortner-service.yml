apiVersion: v1
kind: Service
metadata:
  name: shortner-service
  namespace: url-shortner
spec:
  selector:
    app: shortner
  ports:
    - protocol: TCP
      port: 3002
      targetPort: 3002
  type: ClusterIP
